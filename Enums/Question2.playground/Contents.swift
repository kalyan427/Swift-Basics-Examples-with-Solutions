
/* Create an enumeration named "MathOperation" with cases for addition, subtraction, multiplication, and division. Include associated values to hold the operands */

import Cocoa

enum MathOperation {
    case addition(Int, Int)
    case subtraction(Int, Int)
    case multiplication(Int, Int)
    case division(Int, Int)
}

    









